import { combineReducers } from 'redux';
import treeData from './treeData';

// export const reducerObject = { TreeDataType };
export const combinedReducers = combineReducers({ treeData });

export default combinedReducers;
